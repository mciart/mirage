[server]
host = "localhost"
port = 55155

[authentication]
username = "test"
password = "test"
trusted_certificate_paths = ["tests/static/server_cert.pem"]

[connection]
mtu = 1420

[log]
level = "info"
